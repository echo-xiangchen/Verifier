(declare-const p Bool)
(declare-const q Bool)
(declare-const a (Array Int Bool))
(assert (= (select a 1) p))
(assert (= (select a 2) q))
(assert (= (select a 3) (and p q)))
(assert (not (select a 1)))
(check-sat)
;Uncomment the following line if the result of z3 online tool returns "sat"
;(get-model)
